<?xml version="1.0" encoding="utf-8"?>
<font-family xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Regular -->
    <font
        android:font="@font/Metropolis-Regular.otf"
        android:fontStyle="normal"
        android:fontWeight="400" />

    <!-- Negrita -->
    <font
        android:font="@font/Metropolis-Bold"
        android:fontStyle="normal"
        android:fontWeight="700" />

    <!-- Itálica -->
    <font
        android:font="@font/Metropolis-Italic"
        android:fontStyle="italic"
        android:fontWeight="400" />

    <!-- Negrita e Itálica -->
    <font
        android:font="@font/Metropolis-BoldItalic"
        android:fontStyle="italic"
        android:fontWeight="700" />

</font-family>
